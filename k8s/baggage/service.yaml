apiVersion: v1
kind: Service
metadata:
  name: baggage-api
  namespace: baggage
spec:
  type: NodePort
  selector:
    app: baggage-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30501
