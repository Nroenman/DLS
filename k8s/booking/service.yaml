apiVersion: v1
kind: Service
metadata:
  name: booking
  namespace: airport
  labels:
    app: booking
spec:
  selector:
    app: booking
  ports:
    - name: http
      port: 8080
      targetPort: 8080
