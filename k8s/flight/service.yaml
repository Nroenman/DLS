apiVersion: v1
kind: Service
metadata:
  name: flight
  namespace: airport
  labels:
    app: flight
spec:
  selector:
    app: flight
  ports:
    - name: http
      port: 8080
      targetPort: 8080
