apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: airport
  labels:
    app: ui
spec:
  type: NodePort
  selector:
    app: ui
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
